# Device connection
device:
  host: '10.44.40.218'
  port: 7734
  user_id: 'wroot'
  password: 'wroot'
  api_version: 'API2'    # 'LGIN' for API v1, 'API2' for API v2
  ssh_host: ''            # Defaults to host if empty
  ssh_port: 22

# pyWNMS Settings
# Connection timers (seconds)
session_timeout: 20.0
connect_timeout: 5.0
validate_connection_timer: 60.0

# Trigger holdoff timers (seconds)
trig_email_holdoff: 5.0
trig_spectrum_holdoff: 5.0
trig_tpwr_holdoff: 5.0

# Logging
logger_size: 100
log_file_date_format: '%Y-%m'

# Last opened project path
last_project_path: ''

# SMTP Email Configuration
email:
  server: 'proximion-com.mail.protection.outlook.com'            # SMTP server hostname, e.g. 'smtp.example.com'
  port: 25              # SMTP port (25, 465, 587)
  sender: 'wnms@proximion.com'            # From address, e.g. 'wnms@example.com'
  recipients:           # List of recipient addresses
    - 'emir.alwan@proximion.com'
    - 'it-admin@proximion.com'
